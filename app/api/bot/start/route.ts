import { NextResponse } from 'next/server'; import * as KV from '@/lib/store/volatile';  export const runtime = 'nodejs'; export const dynamic = 'force-dynamic';  export async function POST(req: Request) {   let body: any = {};   try { body = await req.json(); } catch {}   const level = String(body?.level ?? 'standard');   const status = 'active';    // Bot aktivieren + Status setzen (best-effort)   try {     if (typeof KV.kvSet === 'function') KV.kvSet('bot.active', true);     if (typeof KV.set === 'function')   KV.set('bot.active', true);   } catch {}    try {     if (typeof KV.kvSet === 'function') KV.kvSet('bot:status', { status, level });   } catch {}    return NextResponse.json({ ok: true, status, level }); }  export async function GET() {   const active =     typeof KV.getBoolean === 'function'       ? KV.getBoolean('bot.active', true)       : true;   return NextResponse.json({ ok: true, status: active ? 'active' : 'inactive', active }); }